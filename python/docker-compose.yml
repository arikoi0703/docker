version: "3"
services:
    tweebot:
        build: ./tweebot 
        container_name: tweebot 
        volumes:
            - ./tweebot/script:/script:ro
        command:
            sh -c "python3 main.py"
        restart: unless-stopped

